import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontWeight:"bold",
    textAlign:"center",
    color: "#E5BF3C",
    fontSize: 30
  },
  image: {
    height:180,
    resizeMode:"contain"
  }
});